spring:
  profiles:
    active: dev
  datasource:
    name: gateway
    url: jdbc:mysql://localhost:3306/gateway?characterEncoding=utf-8&useSSL=false&useTimezone=true&serverTimezone=Asia/Shanghai&connectTimeout=5000&socketTimeout=30000
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jdbc:
    template:
      query-timeout: 3
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0
      password:
      timeout: 3s
